<% @books.each do |book| %>
  <p><%= link_to(book.title, book) %></p>
  <%= image_tag(book.image) %>
  <%= link_to("Edit", edit_book_path(book)) %>
  <%= link_to("Delete", book_path(book), data:{method: :delete, confirm: "Are you sure you want to delete #{book.title}"}) %>
<% end %>