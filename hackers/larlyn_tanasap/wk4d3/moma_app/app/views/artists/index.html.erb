<ul>
  <% @artists.each do |artist| %>
    <li>
    <%= link_to( artist.name, artist ) %> <%= link_to("Delete", artist, :method => :delete, :data => {:confirm => "Are you sure you want to delete #{artist.name}?"} )%>
    <%= link_to("Edit", edit_artist_path( artist ) )%>
    </li>
  <% end %>
</ul>