<h2>Authors</h2>

<% @authors.each do |author| %>
  <li>
  <%= link_to( author.name, author ) %>
  <%= link_to('Edit', edit_author_path(author) ) %>
  <%= link_to( 'Remove', author, :method => :delete, :data => {:confirm => "Are you sure you want to remove #{ author.name }?"} ) %>
  </li>
<% end %>